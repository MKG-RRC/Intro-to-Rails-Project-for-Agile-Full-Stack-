<!DOCTYPE html>
<html>
  <head>
    <title>UniCatalog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!--  Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <!-- 🌐 Responsive Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
      <div class="container-fluid">
        <!-- Brand -->
        <%= link_to "UniCatalog", root_path, class: "navbar-brand fw-bold" %>

        <!-- Mobile Toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Universities", universities_path, class: "nav-link #{'active' if current_page?(universities_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Programs", programs_path, class: "nav-link #{'active' if current_page?(programs_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Departments", departments_path, class: "nav-link #{'active' if current_page?(departments_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "About", about_path, class: "nav-link #{'active' if current_page?(about_path)}" %>
            </li>
          </ul>

          <!--  Search Form -->
          <%= form_with url: universities_path, method: :get, local: true, class: "d-flex" do %>
            <%= text_field_tag :q, params[:q], placeholder: "Search universities...", class: "form-control me-2" %>
            <%= submit_tag "Search", class: "btn btn-outline-light" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!--  Main Content -->
    <div class="container my-4">
      <%= yield %>
    </div>

    <!--  Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
